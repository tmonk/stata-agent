# Power Checklist

State:

- estimand,
- variance assumptions,
- effect size or MDE,
- alpha and power targets,
- cluster structure when relevant.

Do not blur ex ante design calculations with ex post excuses for noisy estimates.

Use `stata run` to execute power commands (e.g., `power twomeans`, `power regress`) and `stata results --return r` to retrieve computed values.
